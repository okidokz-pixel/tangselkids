"use client";
// Unified premium gate — re-exports the canonical FilterGateSheet so all
// premium bottom sheets share one implementation.
export { FilterGateSheet as SaveGateSheet } from "./FilterGateSheet";
